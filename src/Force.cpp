/// @file Force.cpp
/// @brief Defines the methods to add forces and calculate the acceleration, velocity and positions

/*#include <vector>
#include <math.h>
#include <QtDebug>
#include <ngl/Vec3.h>
#include "Force.h"
#include "Boid.h"
#include "Flock.h"



Force::Force()
{
  v1 = m_flock->boidCohesion(m_flock->BoidList[i]);
  v2 = m_flock->boidSeparation(m_flock->BoidList[i]);
  v3 = m_flock->boidAlignment(m_flock->BoidList[i]);
  p1 = m_flock->boundingBox(m_flock->BoidList[i]);
  p2 = m_flock->tendToPlace(m_flock->BoidList[i]);
}



void Force::addForces(Flock* m_flock)
{

}
*/
